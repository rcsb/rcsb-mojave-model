{
  "type": "object",
  "$schema" : "http://json-schema.org/draft-04/schema#",
  "title" : "core_go.json",
  "required" : [ "rcsb_go_container_identifiers" ],
  "properties": {
    "rcsb_go_container_identifiers" : {
      "type" : "object",
      "properties": {
        "go_id": {
          "type" : "string",
          "description": "Unique GO identifier."
        }
      }
    },
    "id": {
      "type": "string",
      "examples": ["GO:0005125", "GO:0060092"],
      "description": "A unique seven digit identifier prefixed by GO:."
    },
    "aspect": {
      "type": "string",
      "enum": [
        "cellular_component",
        "biological_process",
        "molecular_function"
      ],
      "description": "Denotes which of the three sub-ontologies (cellular component, biological process or molecular function) the term belongs to."
    },
    "description": {
      "type": "string",
      "description": "A textual description of what the term represents."
    },
    "label": {
      "type": "string",
      "examples": [ "response to muscle activity", "sulfur utilization", "cell killing"],
      "description": "A human-readable term name."
    },
    "synonyms": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string"
      }
    }
  },
  "description": "Controlled vocabularies (ontologies) that describe gene products in terms of their associated \nbiological processes, cellular components and molecular functions in a species-independent manner."
}
